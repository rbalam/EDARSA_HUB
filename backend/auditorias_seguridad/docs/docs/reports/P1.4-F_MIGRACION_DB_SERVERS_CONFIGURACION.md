# P1.4-F: MIGRACIÓN DB.SERVERS MÓDULO CONFIGURACIÓN — COMPLETADO

**Fecha:** 14-Dic-2025  
**Estado:** ✅ COMPLETADO Y VALIDADO  
**Autorizado por:** Usuario (Mensaje 14-Dic-2025)

---

## 1. RESUMEN

Se migraron exitosamente las 2 referencias a `db.servers` en el módulo `modules/configuracion/*`, eliminando la dependencia directa de MongoDB y usando `server_registry` (fuente: EDARSAHUB SQL).

---

## 2. ARCHIVOS MODIFICADOS

| Archivo | Línea | Función | Antes | Después |
|---------|-------|---------|-------|---------|
| `config_asignaciones_repository.py` | 249 | `actualizar()` | `self.db.servers.find_one()` | `server_registry.get_server_by_id()` |
| `almacenes_sync_service.py` | 138 | `sincronizar_almacenes_desde_origen()` | `db.servers.find_one()` | `server_registry.get_server_connection_info_with_secrets()` |

---

## 3. DETALLE DE CAMBIOS

### 3.1 `config_asignaciones_repository.py`

**Propósito:** Obtener `sucursal_origen_id` del servidor al actualizar una configuración de asignación.

**Antes:**
```python
server = await self.db.servers.find_one(
    {"id": server_id},
    {"_id": 0, "sucursal_origen_id": 1}
)
```

**Después:**
```python
from core.server_registry import get_server_by_id
server = await get_server_by_id(server_id, db=self.db, mask_secrets=True)
```

**Campos usados:** `sucursal_origen_id`

---

### 3.2 `almacenes_sync_service.py`

**Propósito:** Obtener credenciales del servidor para ejecutar query de sincronización de almacenes.

**Antes:**
```python
server = await db.servers.find_one({"id": server_id}, {"_id": 0})
```

**Después:**
```python
from core.server_registry import get_server_connection_info_with_secrets
server = get_server_connection_info_with_secrets(server_id)
```

**Campos usados:** `host`, `port`, `database`, `username`, `password`

---

## 4. EVIDENCIA GREP ANTES/DESPUÉS

### ANTES:

```
modules/configuracion/repositories/config_asignaciones_repository.py:249:    server = await self.db.servers.find_one(
modules/configuracion/services/almacenes_sync_service.py:138:    server = await db.servers.find_one({"id": server_id}, {"_id": 0})
```

### DESPUÉS:

```bash
$ grep -rn "await.*db\.servers\|await self\.db\.servers" /app/backend/modules/configuracion --include="*.py" | grep -v "# ANTES"
(vacío - solo comentarios de documentación)
```

✅ **0 referencias activas a `db.servers` en modules/configuracion**

---

## 5. VALIDACIÓN NO REGRESIÓN

```bash
# 1. Login
✅ OK - Token obtenido

# 2. GET /api/servers
✅ 200 OK - 8 servidores

# 3. GET /api/comercial/tablero-ejecutivo
✅ 200 OK - 5 unidades

# 4. GET /api/catalogos/dominios
✅ 200 OK - success: True

# 5. server.py
✅ 0 referencias activas (45 son comentarios # ANTES)
```

---

## 6. FUNCIONES DEL REGISTRY UTILIZADAS

| Función | Archivo | Propósito |
|---------|---------|-----------|
| `get_server_by_id()` | `server_registry.py` | Obtener servidor con secretos enmascarados |
| `get_server_connection_info_with_secrets()` | `server_registry.py` | Obtener servidor con secretos descifrados para conexión SQL |

---

## 7. SEGURIDAD VERIFICADA

| Aspecto | Estado |
|---------|--------|
| `config_asignaciones_repository.py` usa `mask_secrets=True` | ✅ No expone password |
| `almacenes_sync_service.py` usa secretos solo internamente | ✅ No expone al frontend |
| MongoDB no usado como fuente | ✅ |

---

## 8. PROGRESO TOTAL MIGRACIÓN `db.servers`

| Componente | Referencias Activas | Estado |
|------------|---------------------|--------|
| **server.py** | 0 | ✅ 100% MIGRADO |
| **modules/configuracion** | 0 | ✅ 100% MIGRADO |
| modules/comercial | 1 (legacy EDARSAHUB_SERVER_ID) | ⏸️ Legacy controlado |
| core/scheduler/jobs | 3 | ⏸️ Fase posterior |
| core/* (otros) | 7 | ⏸️ Fase 3+ |
| routes/portal_proveedores | 2 | ⏸️ P2 |
| scripts/* | 8 | ⏸️ No productivo |

---

## 9. CONFIRMACIÓN EXPLÍCITA

- ✅ modules/configuracion/* tiene 0 referencias activas a db.servers
- ✅ El módulo configuración sigue funcionando
- ✅ No se exponen secretos al frontend
- ✅ No se tocó auth/login/RBAC
- ✅ No se rompió Tablero Ejecutivo (5 unidades)
- ✅ No se rompió Comercial V2
- ✅ /api/servers sigue leyendo desde EDARSAHUB SQL
- ✅ server.py sigue con 0 referencias activas

---

**CIERRE:** P1.4-F completado sin regresiones. `modules/configuracion` ya no depende de `db.servers`.

*Documento generado bajo régimen de Autorización Controlada.*
