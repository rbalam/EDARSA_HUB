===== 1. CONTROL =====
RAMA=Edarsahub_Desarrollo
HEAD=3dfba587c0b20a0a23537d12f8258dc0076206b4
TARGET_SHA_ANTES=cb2620a2a008c003f5bbad3cac2f8deb83e6158d176bbd11913aab8ec6ef8cee
STAGING_ANTES:
VACIO
STATUS_TARGET_ANTES:
LIMPIO
CONTROL=OK

===== 2. CONSTRUIR CANDIDATO POR AST Y RANGOS EXACTOS =====
CANDIDATE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/sync_comercial_endpoints_job.py
EDITS_JSON=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/edits.json
EDITS_TOTAL=15
LINEAS=480
BYTES=20679
TRANSFORMACION=AST_RANGOS_EXACTOS
INICIO_BUCLES=COMIENZO_FISICO_DE_LINEA
REGEX_ESTRUCTURAL=NO
LIBCST_REQUERIDO=NO
CUERPOS_NEGOCIO_PRESERVADOS=SI
SYNCS_TRANSFORMADOS=4
METODOS_LEGACY_ELIMINADOS=2
ARCHIVO_REAL_MODIFICADO=NO
CANDIDATO_AST_LINE_START=CONSTRUIDO

===== 3. COMPILAR CANDIDATO =====
PY_COMPILE=PASS

===== 4. VALIDACION ESTRUCTURAL POSTERIOR =====
_get_active_servers_ELIMINADO=SI
_get_sucursales_por_server_ELIMINADO=SI
_sync_metas_comerciales|ARG=contexts|ANNOTATION=List[PosRuntimeContext]|CONTEXT_LOOPS=1|SUC_ADAPTERS=1|SERVER_ASSIGNMENTS=1|LEGACY_CALLS=0
_sync_ticket_perfecto|ARG=contexts|ANNOTATION=List[PosRuntimeContext]|CONTEXT_LOOPS=1|SUC_ADAPTERS=1|SERVER_ASSIGNMENTS=1|LEGACY_CALLS=0
_sync_mesas|ARG=contexts|ANNOTATION=List[PosRuntimeContext]|CONTEXT_LOOPS=1|SUC_ADAPTERS=1|SERVER_ASSIGNMENTS=1|LEGACY_CALLS=0
_sync_pax_detalle|ARG=contexts|ANNOTATION=List[PosRuntimeContext]|CONTEXT_LOOPS=1|SUC_ADAPTERS=1|SERVER_ASSIGNMENTS=1|LEGACY_CALLS=0
LIST_POS_RUNTIME_CONTEXTS_CALLS=1
IMPORT_CANONICO=SI
LEGACY_FRAGMENT|VALUE=FROM Servidores_Conexiones|PRESENTE=NO
LEGACY_FRAGMENT|VALUE=FROM dbo.Servidores_Conexiones|PRESENTE=NO
LEGACY_FRAGMENT|VALUE=Sistema_SucursalServidorMapeo|PRESENTE=NO
LEGACY_FRAGMENT|VALUE=server["ServerID"]|PRESENTE=NO
LEGACY_FRAGMENT|VALUE=for server in servers|PRESENTE=NO
LEGACY_FRAGMENT|VALUE=for suc in sucursales|PRESENTE=NO
REQUIRED_FRAGMENT|VALUE=contexts = list_pos_runtime_contexts()|PRESENTE=SI
REQUIRED_FRAGMENT|VALUE=for context in contexts:|PRESENTE=SI
REQUIRED_FRAGMENT|VALUE=context.server_id|PRESENTE=SI
REQUIRED_FRAGMENT|VALUE=context.sucursal_origen_id|PRESENTE=SI
REQUIRED_FRAGMENT|VALUE="SucursalID"|PRESENTE=SI
REQUIRED_FRAGMENT|VALUE="SucursalNombre"|PRESENTE=SI
VALIDACION_AST_POSTERIOR=PASS
METODOS_SYNC_VALIDOS=4

===== 5. VALIDAR PRESERVACION DEL CUERPO DE NEGOCIO =====
_sync_metas_comerciales|CURSOR_EXECUTE_ANTES=2|CURSOR_EXECUTE_DESPUES=2|COMMIT_ANTES=1|COMMIT_DESPUES=1|ROLLBACK_ANTES=0|ROLLBACK_DESPUES=0
_sync_ticket_perfecto|CURSOR_EXECUTE_ANTES=2|CURSOR_EXECUTE_DESPUES=2|COMMIT_ANTES=1|COMMIT_DESPUES=1|ROLLBACK_ANTES=0|ROLLBACK_DESPUES=0
_sync_mesas|CURSOR_EXECUTE_ANTES=2|CURSOR_EXECUTE_DESPUES=2|COMMIT_ANTES=1|COMMIT_DESPUES=1|ROLLBACK_ANTES=0|ROLLBACK_DESPUES=0
_sync_pax_detalle|CURSOR_EXECUTE_ANTES=2|CURSOR_EXECUTE_DESPUES=2|COMMIT_ANTES=1|COMMIT_DESPUES=1|ROLLBACK_ANTES=0|ROLLBACK_DESPUES=0
PRESERVACION_CUERPO_NEGOCIO=PASS

===== 6. GENERAR DIFF =====
DIFF_FILE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/candidate.diff
DIFF_LINEAS=572
DIFF_BYTES=30517

===== 7. DIFF COMPLETO =====
--- a/backend/core/scheduler/jobs/sync_comercial_endpoints_job.py
+++ b/backend/core/scheduler/jobs/sync_comercial_endpoints_job.py
@@ -26,6 +26,7 @@
 
 from ..config import JobConfig
 from core.sql_first.db import get_sql_connection
+from core.connections.pos_runtime_resolver import PosRuntimeContext, list_pos_runtime_contexts
 
 logger = logging.getLogger(__name__)
 
@@ -103,11 +104,11 @@
         
         try:
             # Obtener lista de servidores activos
-            servers = self._get_active_servers()
+            contexts = list_pos_runtime_contexts()
             
-            if not servers:
+            if not contexts:
                 logger.warning(f"[{self.JOB_ID}] No hay servidores activos")
-                results["message"] = "No hay servidores activos"
+                results["message"] = "No hay contextos POS activos"
                 return results
             
             # Sincronizar cada tipo de dato
@@ -120,7 +121,7 @@
             
             for sync_name, sync_method in sync_methods:
                 try:
-                    count = sync_method(servers)
+                    count = sync_method(contexts)
                     results["details"][sync_name] = {"success": True, "count": count}
                     results["success_count"] += 1
                     results["processed_count"] += count
@@ -156,55 +157,11 @@
     # HELPERS
     # =========================================================================
     
-    def _get_active_servers(self) -> List[Dict]:
-        """Obtiene servidores activos desde EDARSAHUB."""
-        try:
-            conn = self._get_edarsahub_connection()
-            cursor = conn.cursor()
-            
-            cursor.execute("""
-                SELECT DISTINCT id as id, nombre as ServerNombre
-                FROM Servidores_Conexiones
-                WHERE activo = 1
-            """)
-            
-            servers = list(cursor.fetchall())
-            cursor.close()
-            
-            logger.info(f"[{self.JOB_ID}] Servidores activos: {len(servers)}")
-            return servers
-            
-        except Exception as e:
-            logger.error(f"[{self.JOB_ID}] Error obteniendo servidores: {e}")
-            return []
-    
-    def _get_sucursales_por_server(self, server_id: str) -> List[Dict]:
-        """Obtiene sucursales de un servidor."""
-        try:
-            conn = self._get_edarsahub_connection()
-            cursor = conn.cursor()
-            
-            # Usar MongoServidorUUID para hacer match con el ID del servidor
-            cursor.execute("""
-                SELECT SucursalID, SucursalOrigenID, SucursalID as SucursalNombre
-                FROM Sistema_SucursalServidorMapeo
-                WHERE (ServidorID = %s OR MongoServidorUUID = %s) AND Activo = 1
-            """, (server_id, server_id))
-            
-            sucursales = list(cursor.fetchall())
-            cursor.close()
-            
-            return sucursales
-            
-        except Exception as e:
-            logger.warning(f"[{self.JOB_ID}] Error obteniendo sucursales: {e}")
-            return []
-    
     # =========================================================================
     # SYNC: METAS COMERCIALES
     # =========================================================================
     
-    def _sync_metas_comerciales(self, servers: List[Dict]) -> int:
+    def _sync_metas_comerciales(self, contexts: List[PosRuntimeContext]) -> int:
         """
         Sincroniza metas comerciales desde KPIs existentes.
         Genera metas basadas en datos históricos.
@@ -219,83 +176,87 @@
         mes = now.month
         count = 0
         
-        for server in servers:
-            server_id = server["ServerID"]
-            sucursales = self._get_sucursales_por_server(server_id)
-            
-            for suc in sucursales:
-                try:
-                    # Obtener ventas actuales del mes desde KPIs (columnas en minúsculas)
-                    cursor.execute("""
-                        SELECT 
-                            ISNULL(SUM(ventas_total), 0) as VentaBrutaActual,
-                            ISNULL(AVG(ticket_promedio), 0) as TicketPromedioActual,
-                            ISNULL(SUM(tickets_total), 0) as CuentasActual,
-                            ISNULL(SUM(pax_total), 0) as ComensalesActual,
-                            COUNT(DISTINCT fecha_operacion) as DiasTranscurridos
-                        FROM Comercial_KPIs_Diarios_v2
-                        WHERE server_id = %s 
-                          AND sucursal_id = %s
-                          AND anio = %s
-                          AND mes = %s
-                    """, (server_id, suc["SucursalID"], anio, mes))
+        for context in contexts:
+            server_id = str(context.server_id)
+            suc = {
+                "SucursalID": str(
+                    context.sucursal_origen_id or "DEFAULT"
+                ).strip(),
+                "SucursalNombre": context.unidad_nombre,
+            }
+            try:
+                # Obtener ventas actuales del mes desde KPIs (columnas en minúsculas)
+                cursor.execute("""
+                    SELECT 
+                        ISNULL(SUM(ventas_total), 0) as VentaBrutaActual,
+                        ISNULL(AVG(ticket_promedio), 0) as TicketPromedioActual,
+                        ISNULL(SUM(tickets_total), 0) as CuentasActual,
+                        ISNULL(SUM(pax_total), 0) as ComensalesActual,
+                        COUNT(DISTINCT fecha_operacion) as DiasTranscurridos
+                    FROM Comercial_KPIs_Diarios_v2
+                    WHERE server_id = %s 
+                      AND sucursal_id = %s
+                      AND anio = %s
+                      AND mes = %s
+                """, (server_id, suc["SucursalID"], anio, mes))
                     
-                    row = cursor.fetchone()
+                row = cursor.fetchone()
                     
-                    if row:
-                        meta_id = f"{server_id}-{suc['SucursalID']}-{anio}-{mes}"
+                if row:
+                    meta_id = f"{server_id}-{suc['SucursalID']}-{anio}-{mes}"
                         
-                        # Calcular meta basada en promedio histórico + 10%
-                        venta_actual = float(row["VentaBrutaActual"] or 0)
-                        dias = int(row["DiasTranscurridos"] or 1)
-                        dias_mes = 30  # Aproximado
-                        
-                        # Proyección simple
-                        promedio_diario = venta_actual / dias if dias > 0 else 0
-                        proyeccion = promedio_diario * dias_mes
-                        meta = proyeccion * 1.10  # Meta = proyección + 10%
-                        
-                        # Upsert en tabla - simplificado
-                        cursor.execute("""
-                            IF NOT EXISTS (SELECT 1 FROM Sync_Metas_Comerciales 
-                                           WHERE ServerID = %s AND SucursalID = %s AND Anio = %s AND Mes = %s)
-                                INSERT INTO Sync_Metas_Comerciales 
-                                (MetaID, ServerID, SucursalID, SucursalNombre, Anio, Mes,
-                                 MetaVentaBruta, VentaBrutaActual, VentaNetaActual, 
-                                 TicketPromedioActual, CuentasActual, ComensalesActual,
-                                 DiasTranscurridos, DiasRestantes, ProyeccionMes)
-                                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
-                            ELSE
-                                UPDATE Sync_Metas_Comerciales SET
-                                    VentaBrutaActual = %s,
-                                    VentaNetaActual = %s,
-                                    TicketPromedioActual = %s,
-                                    CuentasActual = %s,
-                                    ComensalesActual = %s,
-                                    DiasTranscurridos = %s,
-                                    DiasRestantes = %s,
-                                    ProyeccionMes = %s,
-                                    FechaSync = GETUTCDATE()
-                                WHERE ServerID = %s AND SucursalID = %s AND Anio = %s AND Mes = %s
-                        """, (
-                            # EXISTS check params
-                            server_id, suc["SucursalID"], anio, mes,
-                            # INSERT params
-                            meta_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
-                            anio, mes, meta, row["VentaBrutaActual"], row["VentaNetaActual"],
-                            row["TicketPromedioActual"], row["CuentasActual"],
-                            row["ComensalesActual"], dias, dias_mes - dias, proyeccion,
-                            # UPDATE params
-                            row["VentaBrutaActual"], row["VentaNetaActual"],
-                            row["TicketPromedioActual"], row["CuentasActual"],
-                            row["ComensalesActual"], dias, dias_mes - dias, proyeccion,
-                            server_id, suc["SucursalID"], anio, mes
-                        ))
-                        conn.commit()
-                        count += 1
+                    # Calcular meta basada en promedio histórico + 10%
+                    venta_actual = float(row["VentaBrutaActual"] or 0)
+                    dias = int(row["DiasTranscurridos"] or 1)
+                    dias_mes = 30  # Aproximado
+                        
+                    # Proyección simple
+                    promedio_diario = venta_actual / dias if dias > 0 else 0
+                    proyeccion = promedio_diario * dias_mes
+                    meta = proyeccion * 1.10  # Meta = proyección + 10%
                         
-                except Exception as e:
-                    logger.warning(f"[{self.JOB_ID}] Error sync metas {suc.get('SucursalID')}: {e}")
+                    # Upsert en tabla - simplificado
+                    cursor.execute("""
+                        IF NOT EXISTS (SELECT 1 FROM Sync_Metas_Comerciales 
+                                       WHERE ServerID = %s AND SucursalID = %s AND Anio = %s AND Mes = %s)
+                            INSERT INTO Sync_Metas_Comerciales 
+                            (MetaID, ServerID, SucursalID, SucursalNombre, Anio, Mes,
+                             MetaVentaBruta, VentaBrutaActual, VentaNetaActual, 
+                             TicketPromedioActual, CuentasActual, ComensalesActual,
+                             DiasTranscurridos, DiasRestantes, ProyeccionMes)
+                            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
+                        ELSE
+                            UPDATE Sync_Metas_Comerciales SET
+                                VentaBrutaActual = %s,
+                                VentaNetaActual = %s,
+                                TicketPromedioActual = %s,
+                                CuentasActual = %s,
+                                ComensalesActual = %s,
+                                DiasTranscurridos = %s,
+                                DiasRestantes = %s,
+                                ProyeccionMes = %s,
+                                FechaSync = GETUTCDATE()
+                            WHERE ServerID = %s AND SucursalID = %s AND Anio = %s AND Mes = %s
+                    """, (
+                        # EXISTS check params
+                        server_id, suc["SucursalID"], anio, mes,
+                        # INSERT params
+                        meta_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
+                        anio, mes, meta, row["VentaBrutaActual"], row["VentaNetaActual"],
+                        row["TicketPromedioActual"], row["CuentasActual"],
+                        row["ComensalesActual"], dias, dias_mes - dias, proyeccion,
+                        # UPDATE params
+                        row["VentaBrutaActual"], row["VentaNetaActual"],
+                        row["TicketPromedioActual"], row["CuentasActual"],
+                        row["ComensalesActual"], dias, dias_mes - dias, proyeccion,
+                        server_id, suc["SucursalID"], anio, mes
+                    ))
+                    conn.commit()
+                    count += 1
+                        
+            except Exception as e:
+                logger.warning(f"[{self.JOB_ID}] Error sync metas {suc.get('SucursalID')}: {e}")
+
         
         cursor.close()
         logger.info(f"[{self.JOB_ID}] Metas sincronizadas: {count}")
@@ -305,7 +266,7 @@
     # SYNC: TICKET PERFECTO
     # =========================================================================
     
-    def _sync_ticket_perfecto(self, servers: List[Dict]) -> int:
+    def _sync_ticket_perfecto(self, contexts: List[PosRuntimeContext]) -> int:
         """
         Sincroniza datos de ticket perfecto desde KPIs.
         """
@@ -318,52 +279,56 @@
         fecha_inicio = datetime.now(timezone.utc).date() - timedelta(days=7)
         count = 0
         
-        for server in servers:
-            server_id = server["ServerID"]
-            sucursales = self._get_sucursales_por_server(server_id)
-            
-            for suc in sucursales:
-                try:
-                    cursor.execute("""
-                        SELECT 
-                            fecha_operacion as fecha_operacion,
-                            ISNULL(tickets_total, 0) as TotalCuentas,
-                            ISNULL(pax_total, 0) as TotalComensales,
-                            ISNULL(ventas_total, 0) as VentaTotal,
-                            ISNULL(ticket_promedio, 0) as TicketPromedioReal
-                        FROM Comercial_KPIs_Diarios_v2
-                        WHERE server_id = %s 
-                          AND sucursal_id = %s
-                          AND fecha_operacion >= %s
-                    """, (server_id, suc["SucursalID"], fecha_inicio))
+        for context in contexts:
+            server_id = str(context.server_id)
+            suc = {
+                "SucursalID": str(
+                    context.sucursal_origen_id or "DEFAULT"
+                ).strip(),
+                "SucursalNombre": context.unidad_nombre,
+            }
+            try:
+                cursor.execute("""
+                    SELECT 
+                        fecha_operacion as fecha_operacion,
+                        ISNULL(tickets_total, 0) as TotalCuentas,
+                        ISNULL(pax_total, 0) as TotalComensales,
+                        ISNULL(ventas_total, 0) as VentaTotal,
+                        ISNULL(ticket_promedio, 0) as TicketPromedioReal
+                    FROM Comercial_KPIs_Diarios_v2
+                    WHERE server_id = %s 
+                      AND sucursal_id = %s
+                      AND fecha_operacion >= %s
+                """, (server_id, suc["SucursalID"], fecha_inicio))
                     
-                    for row in cursor.fetchall():
-                        ticket_id = f"{server_id}-{suc['SucursalID']}-{row['FechaOperacion']}"
+                for row in cursor.fetchall():
+                    ticket_id = f"{server_id}-{suc['SucursalID']}-{row['FechaOperacion']}"
                         
-                        # Objetivo de ticket perfecto (configurable, default 350)
-                        ticket_objetivo = 350.0
-                        ticket_real = float(row["TicketPromedioReal"] or 0)
-                        cumplimiento = (ticket_real / ticket_objetivo * 100) if ticket_objetivo > 0 else 0
-                        
-                        cursor.execute("""
-                            IF NOT EXISTS (SELECT 1 FROM Sync_Ticket_Perfecto 
-                                          WHERE ServerID = %s AND SucursalID = %s AND FechaOperacion = %s)
-                                INSERT INTO Sync_Ticket_Perfecto
-                                (TicketID, ServerID, SucursalID, SucursalNombre, FechaOperacion,
-                                 TotalCuentas, TotalComensales, VentaTotal, 
-                                 TicketPromedioReal, TicketPerfectoObjetivo, PorcentajeCumplimiento)
-                                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
-                        """, (
-                            server_id, suc["SucursalID"], row["FechaOperacion"],
-                            ticket_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
-                            row["FechaOperacion"], row["TotalCuentas"], row["TotalComensales"],
-                            row["VentaTotal"], ticket_real, ticket_objetivo, cumplimiento
-                        ))
-                        conn.commit()
-                        count += 1
+                    # Objetivo de ticket perfecto (configurable, default 350)
+                    ticket_objetivo = 350.0
+                    ticket_real = float(row["TicketPromedioReal"] or 0)
+                    cumplimiento = (ticket_real / ticket_objetivo * 100) if ticket_objetivo > 0 else 0
                         
-                except Exception as e:
-                    logger.warning(f"[{self.JOB_ID}] Error sync ticket {suc.get('SucursalID')}: {e}")
+                    cursor.execute("""
+                        IF NOT EXISTS (SELECT 1 FROM Sync_Ticket_Perfecto 
+                                      WHERE ServerID = %s AND SucursalID = %s AND FechaOperacion = %s)
+                            INSERT INTO Sync_Ticket_Perfecto
+                            (TicketID, ServerID, SucursalID, SucursalNombre, FechaOperacion,
+                             TotalCuentas, TotalComensales, VentaTotal, 
+                             TicketPromedioReal, TicketPerfectoObjetivo, PorcentajeCumplimiento)
+                            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
+                    """, (
+                        server_id, suc["SucursalID"], row["FechaOperacion"],
+                        ticket_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
+                        row["FechaOperacion"], row["TotalCuentas"], row["TotalComensales"],
+                        row["VentaTotal"], ticket_real, ticket_objetivo, cumplimiento
+                    ))
+                    conn.commit()
+                    count += 1
+                        
+            except Exception as e:
+                logger.warning(f"[{self.JOB_ID}] Error sync ticket {suc.get('SucursalID')}: {e}")
+
         
         cursor.close()
         logger.info(f"[{self.JOB_ID}] Ticket perfecto sincronizado: {count}")
@@ -373,7 +338,7 @@
     # SYNC: MESAS
     # =========================================================================
     
-    def _sync_mesas(self, servers: List[Dict]) -> int:
+    def _sync_mesas(self, contexts: List[PosRuntimeContext]) -> int:
         """
         Sincroniza estado de mesas desde ventas abiertas.
         """
@@ -385,51 +350,55 @@
         hoy = datetime.now(timezone.utc).date()
         count = 0
         
-        for server in servers:
-            server_id = server["ServerID"]
-            sucursales = self._get_sucursales_por_server(server_id)
-            
-            for suc in sucursales:
-                try:
-                    # Obtener datos agregados de ventas (las columnas reales son minúsculas)
-                    cursor.execute("""
-                        SELECT 
-                            sucursal_id,
-                            sucursal_nombre,
-                            CAST(fecha_operacion as DATE) as FechaOp,
-                            ISNULL(SUM(tickets_cerrados_dia), 0) as TotalCuentas,
-                            ISNULL(SUM(pax_cerrados_dia), 0) as TotalComensales,
-                            ISNULL(SUM(ventas_cerradas_dia), 0) as VentaTotal
-                        FROM Comercial_Ventas_Dia_Abiertas_v2
-                        WHERE server_id = %s AND sucursal_id = %s
-                          AND CAST(fecha_operacion AS DATE) = %s
-                        GROUP BY sucursal_id, sucursal_nombre, CAST(fecha_operacion as DATE)
-                    """, (server_id, suc["SucursalID"], hoy))
+        for context in contexts:
+            server_id = str(context.server_id)
+            suc = {
+                "SucursalID": str(
+                    context.sucursal_origen_id or "DEFAULT"
+                ).strip(),
+                "SucursalNombre": context.unidad_nombre,
+            }
+            try:
+                # Obtener datos agregados de ventas (las columnas reales son minúsculas)
+                cursor.execute("""
+                    SELECT 
+                        sucursal_id,
+                        sucursal_nombre,
+                        CAST(fecha_operacion as DATE) as FechaOp,
+                        ISNULL(SUM(tickets_cerrados_dia), 0) as TotalCuentas,
+                        ISNULL(SUM(pax_cerrados_dia), 0) as TotalComensales,
+                        ISNULL(SUM(ventas_cerradas_dia), 0) as VentaTotal
+                    FROM Comercial_Ventas_Dia_Abiertas_v2
+                    WHERE server_id = %s AND sucursal_id = %s
+                      AND CAST(fecha_operacion AS DATE) = %s
+                    GROUP BY sucursal_id, sucursal_nombre, CAST(fecha_operacion as DATE)
+                """, (server_id, suc["SucursalID"], hoy))
                     
-                    for row in cursor.fetchall():
-                        mesa_id = f"{server_id}-{suc['SucursalID']}-{hoy}-resumen"
+                for row in cursor.fetchall():
+                    mesa_id = f"{server_id}-{suc['SucursalID']}-{hoy}-resumen"
                         
-                        ticket_promedio = (float(row["VentaTotal"] or 0) / int(row["TotalCuentas"] or 1)) if row["TotalCuentas"] else 0
+                    ticket_promedio = (float(row["VentaTotal"] or 0) / int(row["TotalCuentas"] or 1)) if row["TotalCuentas"] else 0
                         
-                        cursor.execute("""
-                            IF NOT EXISTS (SELECT 1 FROM Sync_Mesas 
-                                          WHERE ServerID = %s AND SucursalID = %s 
-                                            AND FechaOperacion = %s AND MesaNumero = %s)
-                                INSERT INTO Sync_Mesas
-                                (MesaRegistroID, ServerID, SucursalID, SucursalNombre, FechaOperacion,
-                                 MesaNumero, TotalCuentas, TotalComensales, VentaTotal, TicketPromedio)
-                                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
-                        """, (
-                            server_id, suc["SucursalID"], hoy, 'TOTAL',
-                            mesa_id, server_id, suc["SucursalID"], row.get("sucursal_nombre", ""),
-                            hoy, 'TOTAL', row["TotalCuentas"], 
-                            row["TotalComensales"], row["VentaTotal"], ticket_promedio
-                        ))
-                        conn.commit()
-                        count += 1
+                    cursor.execute("""
+                        IF NOT EXISTS (SELECT 1 FROM Sync_Mesas 
+                                      WHERE ServerID = %s AND SucursalID = %s 
+                                        AND FechaOperacion = %s AND MesaNumero = %s)
+                            INSERT INTO Sync_Mesas
+                            (MesaRegistroID, ServerID, SucursalID, SucursalNombre, FechaOperacion,
+                             MesaNumero, TotalCuentas, TotalComensales, VentaTotal, TicketPromedio)
+                            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
+                    """, (
+                        server_id, suc["SucursalID"], hoy, 'TOTAL',
+                        mesa_id, server_id, suc["SucursalID"], row.get("sucursal_nombre", ""),
+                        hoy, 'TOTAL', row["TotalCuentas"], 
+                        row["TotalComensales"], row["VentaTotal"], ticket_promedio
+                    ))
+                    conn.commit()
+                    count += 1
                         
-                except Exception as e:
-                    logger.warning(f"[{self.JOB_ID}] Error sync mesas {suc.get('SucursalID')}: {e}")
+            except Exception as e:
+                logger.warning(f"[{self.JOB_ID}] Error sync mesas {suc.get('SucursalID')}: {e}")
+
         
         cursor.close()
         logger.info(f"[{self.JOB_ID}] Mesas sincronizadas: {count}")
@@ -439,7 +408,7 @@
     # SYNC: PAX DETALLE
     # =========================================================================
     
-    def _sync_pax_detalle(self, servers: List[Dict]) -> int:
+    def _sync_pax_detalle(self, contexts: List[PosRuntimeContext]) -> int:
         """
         Sincroniza detalle de PAX (comensales) desde ventas.
         """
@@ -452,50 +421,54 @@
         fecha_inicio = datetime.now(timezone.utc).date() - timedelta(days=7)
         count = 0
         
-        for server in servers:
-            server_id = server["ServerID"]
-            sucursales = self._get_sucursales_por_server(server_id)
-            
-            for suc in sucursales:
-                try:
-                    cursor.execute("""
-                        SELECT 
-                            CAST(fecha_operacion AS DATE) as FechaOperacion,
-                            ISNULL(SUM(pax_cerrados_dia), 0) as TotalComensales,
-                            ISNULL(SUM(ventas_cerradas_dia), 0) as VentaTotal,
-                            ISNULL(SUM(tickets_cerrados_dia), 0) as TotalCuentas
-                        FROM Comercial_Ventas_Dia_Abiertas_v2
-                        WHERE server_id = %s AND sucursal_id = %s
-                          AND fecha_operacion >= %s
-                        GROUP BY CAST(fecha_operacion AS DATE)
-                    """, (server_id, suc["SucursalID"], fecha_inicio))
+        for context in contexts:
+            server_id = str(context.server_id)
+            suc = {
+                "SucursalID": str(
+                    context.sucursal_origen_id or "DEFAULT"
+                ).strip(),
+                "SucursalNombre": context.unidad_nombre,
+            }
+            try:
+                cursor.execute("""
+                    SELECT 
+                        CAST(fecha_operacion AS DATE) as FechaOperacion,
+                        ISNULL(SUM(pax_cerrados_dia), 0) as TotalComensales,
+                        ISNULL(SUM(ventas_cerradas_dia), 0) as VentaTotal,
+                        ISNULL(SUM(tickets_cerrados_dia), 0) as TotalCuentas
+                    FROM Comercial_Ventas_Dia_Abiertas_v2
+                    WHERE server_id = %s AND sucursal_id = %s
+                      AND fecha_operacion >= %s
+                    GROUP BY CAST(fecha_operacion AS DATE)
+                """, (server_id, suc["SucursalID"], fecha_inicio))
                     
-                    for row in cursor.fetchall():
-                        pax_id = f"{server_id}-{suc['SucursalID']}-{row['FechaOperacion']}"
+                for row in cursor.fetchall():
+                    pax_id = f"{server_id}-{suc['SucursalID']}-{row['FechaOperacion']}"
                         
-                        comensales = int(row["TotalComensales"] or 0)
-                        venta = float(row["VentaTotal"] or 0)
-                        consumo_pax = venta / comensales if comensales > 0 else 0
-                        
-                        cursor.execute("""
-                            IF NOT EXISTS (SELECT 1 FROM Sync_PAX_Detalle 
-                                          WHERE ServerID = %s AND SucursalID = %s AND FechaOperacion = %s)
-                                INSERT INTO Sync_PAX_Detalle
-                                (PAXRegistroID, ServerID, SucursalID, SucursalNombre, 
-                                 FechaOperacion, FechaHora, NumeroComensales, 
-                                 VentaCuenta, ConsumoPromedioPAX)
-                                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
-                        """, (
-                            server_id, suc["SucursalID"], row["FechaOperacion"],
-                            pax_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
-                            row["FechaOperacion"], datetime.now(timezone.utc),
-                            comensales, venta, consumo_pax
-                        ))
-                        conn.commit()
-                        count += 1
+                    comensales = int(row["TotalComensales"] or 0)
+                    venta = float(row["VentaTotal"] or 0)
+                    consumo_pax = venta / comensales if comensales > 0 else 0
                         
-                except Exception as e:
-                    logger.warning(f"[{self.JOB_ID}] Error sync PAX {suc.get('SucursalID')}: {e}")
+                    cursor.execute("""
+                        IF NOT EXISTS (SELECT 1 FROM Sync_PAX_Detalle 
+                                      WHERE ServerID = %s AND SucursalID = %s AND FechaOperacion = %s)
+                            INSERT INTO Sync_PAX_Detalle
+                            (PAXRegistroID, ServerID, SucursalID, SucursalNombre, 
+                             FechaOperacion, FechaHora, NumeroComensales, 
+                             VentaCuenta, ConsumoPromedioPAX)
+                            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
+                    """, (
+                        server_id, suc["SucursalID"], row["FechaOperacion"],
+                        pax_id, server_id, suc["SucursalID"], suc.get("SucursalNombre", ""),
+                        row["FechaOperacion"], datetime.now(timezone.utc),
+                        comensales, venta, consumo_pax
+                    ))
+                    conn.commit()
+                    count += 1
+                        
+            except Exception as e:
+                logger.warning(f"[{self.JOB_ID}] Error sync PAX {suc.get('SucursalID')}: {e}")
+
         
         cursor.close()
         logger.info(f"[{self.JOB_ID}] PAX sincronizado: {count}")

===== 8. CONTROL DE INMUTABILIDAD =====
TARGET_SHA_DESPUES=cb2620a2a008c003f5bbad3cac2f8deb83e6158d176bbd11913aab8ec6ef8cee
ARCHIVO_REAL_INTACTO=SI
STAGING_INTACTO=SI

===== 9. RESULTADO =====
CANDIDATO_AST_LINE_START=CONSTRUIDO
TRANSFORMACION=AST_RANGOS_EXACTOS
INICIO_BUCLES=COMIENZO_FISICO_DE_LINEA
REGEX_ESTRUCTURAL=NO
LIBCST_REQUERIDO=NO
CANDIDATE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/sync_comercial_endpoints_job.py
DIFF_FILE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/candidate.diff
EDITS_JSON=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/edits.json
VALIDATION=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/validation.txt
PY_COMPILE=PASS
VALIDACION_AST_POSTERIOR=PASS
PRESERVACION_CUERPO_NEGOCIO=PASS
RESOLVER_CANONICO=list_pos_runtime_contexts
SYNCS_TRANSFORMADOS=4
METODOS_LEGACY_ELIMINADOS=2
ARCHIVO_REAL_MODIFICADO=NO
CODIGO_OPERATIVO_MODIFICADO=NO
SQL_EJECUTADO=NO
SQL_DDL_DML=NO
DATOS_MODIFICADOS=NO
STAGING_MODIFICADO=NO
COMMIT=NO
PUSH=NO
DEPLOY=NO
REPORT=/app/edarsahub_sync_context_ast_line_start_20260805T040022Z.txt
COMPACT=/app/edarsahub_sync_context_ast_line_start_20260805T040022Z_compact.txt
SIGUIENTE_ESTADO=REVISAR_DIFF_Y_CREAR_PRUEBAS_DE_CONTRATO_ANTES_DE_APLICAR
CANDIDATO_AST_LINE_START=CONSTRUIDO
TRANSFORMACION=AST_RANGOS_EXACTOS
INICIO_BUCLES=COMIENZO_FISICO_DE_LINEA
REGEX_ESTRUCTURAL=NO
LIBCST_REQUERIDO=NO
CANDIDATE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/sync_comercial_endpoints_job.py
DIFF_FILE=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/candidate.diff
EDITS_JSON=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/edits.json
VALIDATION=/var/tmp/edarsahub_sync_context_ast_line_start_20260805T040022Z/validation.txt
PY_COMPILE=PASS
VALIDACION_AST_POSTERIOR=PASS
PRESERVACION_CUERPO_NEGOCIO=PASS
RESOLVER_CANONICO=list_pos_runtime_contexts
SYNCS_TRANSFORMADOS=4
METODOS_LEGACY_ELIMINADOS=2
ARCHIVO_REAL_MODIFICADO=NO
CODIGO_OPERATIVO_MODIFICADO=NO
SQL_EJECUTADO=NO
SQL_DDL_DML=NO
DATOS_MODIFICADOS=NO
STAGING_MODIFICADO=NO
COMMIT=NO
PUSH=NO
DEPLOY=NO
REPORT=/app/edarsahub_sync_context_ast_line_start_20260805T040022Z.txt
COMPACT=/app/edarsahub_sync_context_ast_line_start_20260805T040022Z_compact.txt
SIGUIENTE_ESTADO=REVISAR_DIFF_Y_CREAR_PRUEBAS_DE_CONTRATO_ANTES_DE_APLICAR
