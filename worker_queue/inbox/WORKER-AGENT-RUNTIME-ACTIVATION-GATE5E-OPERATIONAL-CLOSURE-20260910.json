{
  "schema": "edarsahub.worker-job.v2",
  "job_id": "WORKER-AGENT-RUNTIME-ACTIVATION-GATE5E-OPERATIONAL-CLOSURE-20260910",
  "created_at_utc": "2026-09-10T15:36:00Z",
  "requested_by": "chatgpt",
  "target_repo": "rbalam/EDARSA_HUB",
  "target_branch": "Edarsahub_Desarrollo",
  "base_sha": "d02a411ec42a90ef1d79ddfa4bec7b0bbc8ff2b5",
  "production_allowed": false,
  "objective": "Cerrar los seis pendientes reales de Runtime Activation sin ampliar privilegios: (1) canario E2E real de red publica controlada usando el executor 5C/5D y la imagen canonica; (2) observabilidad/evidencia runtime sin persistir query, secretos ni payload sensible; (3) servicio superior que encadene requester authorization -> execution gate -> Agent Reach policy -> runtime activation -> executor; (4) limites operativos fail-closed para una sola llamada, timeout <=30s y output <=65536 bytes, con errores normalizados; (5) aislamiento explicito AI vs external research, sin OmniRoute ni proveedor AI runtime; (6) documento de cierre formal de fase. Development/Staging solamente. Produccion prohibida. No SQL writes, no schema changes, no MongoDB, no Worker/Supervisor/watchdog, no RBAC bypass, no URL/command/header/cookie/token/env/mount arbitrario.",
  "human_summary_language": "es",
  "human_summary_level": "13yo-non-programmer",
  "actions": [
    {
      "type": "write_file",
      "path": "backend/core/agent_external_research_service.py",
      "content": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom hashlib import sha256\nfrom pathlib import Path\nfrom time import monotonic\n\nfrom core.agent_capability_policy import PolicyContext, PolicyRequest\nfrom core.agent_execution_gate import authorize_external_research_execution\nfrom core.agent_reach_public_search_executor import (\n    AgentReachPublicSearchResult,\n    execute_agent_reach_public_search,\n)\nfrom core.agent_runtime_activation import (\n    RuntimeActivationMode,\n    RuntimeActivationRequest,\n    RuntimeEnvironment,\n    authorize_runtime_activation,\n)\n\n_MAX_OUTPUT_BYTES = 65536\n\n@dataclass(frozen=True)\nclass ExternalResearchEvidence:\n    allowed: bool\n    success: bool\n    reason: str\n    procedure: str | None\n    effective_scopes: tuple[str, ...]\n    query_sha256: str\n    external_calls_budget: int\n    duration_ms: int\n    stdout_bytes: int\n    stderr_bytes: int\n    returncode: int | None\n    production_touched: bool = False\n\n@dataclass(frozen=True)\nclass ExternalResearchExecution:\n    result: AgentReachPublicSearchResult | None\n    evidence: ExternalResearchEvidence\n\ndef execute_external_research(\n    requester_authorization: object,\n    query: str,\n    request: PolicyRequest,\n    context: PolicyContext,\n    versions_lock_path: str | Path,\n    *,\n    environment: RuntimeEnvironment = RuntimeEnvironment.DEVELOPMENT,\n    timeout_seconds: int = 20,\n    docker_binary: str | None = None,\n) -> ExternalResearchExecution:\n    started = monotonic()\n    query_hash = sha256(str(query).encode('utf-8')).hexdigest()\n    gate = authorize_external_research_execution(\n        requester_authorization, query, request, context\n    )\n    if not gate.allowed or gate.downstream is None:\n        reason = gate.reasons[0] if gate.reasons else 'EXECUTION_GATE_DENIED'\n        return ExternalResearchExecution(\n            None, ExternalResearchEvidence(\n                False, False, reason, None, (), query_hash,\n                request.budgets.external_calls, int((monotonic()-started)*1000),\n                0, 0, None, False,\n            )\n        )\n    activation = authorize_runtime_activation(\n        gate,\n        RuntimeActivationRequest(\n            environment=environment,\n            mode=RuntimeActivationMode.LOCAL_EXECUTE,\n            external_execution_enabled=False,\n        ),\n    )\n    if not activation.allowed or not activation.ready_for_executor:\n        reason = activation.reasons[0] if activation.reasons else 'RUNTIME_NOT_READY_FOR_EXECUTOR'\n        prepared = gate.downstream.prepared\n        return ExternalResearchExecution(\n            None, ExternalResearchEvidence(\n                False, False, reason, prepared.procedure if prepared else None,\n                tuple(sorted(prepared.effective_scopes)) if prepared else (),\n                query_hash, request.budgets.external_calls,\n                int((monotonic()-started)*1000), 0, 0, None, False,\n            )\n        )\n    try:\n        result = execute_agent_reach_public_search(\n            activation, gate.downstream, versions_lock_path,\n            timeout_seconds=timeout_seconds, docker_binary=docker_binary,\n        )\n    except Exception as exc:\n        prepared = gate.downstream.prepared\n        return ExternalResearchExecution(\n            None, ExternalResearchEvidence(\n                True, False, type(exc).__name__,\n                prepared.procedure if prepared else None,\n                tuple(sorted(prepared.effective_scopes)) if prepared else (),\n                query_hash, request.budgets.external_calls,\n                int((monotonic()-started)*1000), 0, 0, None, False,\n            )\n        )\n    stdout_bytes = len(result.stdout.encode('utf-8'))\n    stderr_bytes = len(result.stderr.encode('utf-8'))\n    if stdout_bytes > _MAX_OUTPUT_BYTES:\n        return ExternalResearchExecution(\n            None, ExternalResearchEvidence(\n                True, False, 'AGENT_REACH_OUTPUT_LIMIT_EXCEEDED',\n                gate.downstream.prepared.procedure,\n                tuple(sorted(gate.downstream.prepared.effective_scopes)),\n                query_hash, request.budgets.external_calls,\n                int((monotonic()-started)*1000), stdout_bytes, stderr_bytes,\n                result.returncode, False,\n            )\n        )\n    return ExternalResearchExecution(\n        result, ExternalResearchEvidence(\n            True, result.success, 'OK' if result.success else 'EXTERNAL_EXECUTION_FAILED',\n            gate.downstream.prepared.procedure,\n            tuple(sorted(gate.downstream.prepared.effective_scopes)),\n            query_hash, request.budgets.external_calls,\n            int((monotonic()-started)*1000), stdout_bytes, stderr_bytes,\n            result.returncode, False,\n        )\n    )\n"
    },
    {
      "type": "write_file",
      "path": "backend/tests/test_agent_external_research_service.py",
      "content": "from pathlib import Path\nfrom types import SimpleNamespace\nimport pytest\n\nfrom core.agent_capability_policy import BudgetRequest, DataClassification, PolicyContext, PolicyRequest, RiskLevel\nfrom core.agent_external_research_service import execute_external_research\nfrom core.agent_runtime_activation import RuntimeEnvironment\n\ndef context():\n    caps=frozenset({'EXTERNAL_RESEARCH'}); scopes=frozenset({'external:web'})\n    return PolicyContext(\n        user_allowed_capabilities=caps, agent_allowed_capabilities=caps,\n        environment_allowed_capabilities=caps, policy_allowed_capabilities=caps,\n        user_allowed_scopes=scopes, agent_allowed_scopes=scopes,\n        environment_allowed_scopes=scopes, policy_allowed_scopes=scopes,\n        budget_limits={'runtime_seconds':60,'files_changed':0,'lines_changed':0,'external_calls':1,'sql_rows':0,'inference_cost_usd':0.0},\n    )\n\ndef request(capabilities=frozenset({'EXTERNAL_RESEARCH'}), calls=1):\n    return PolicyRequest(\n        capabilities=capabilities, scopes=frozenset({'external:web'}),\n        risk=RiskLevel.R1, data_classification=DataClassification.PUBLIC,\n        budgets=BudgetRequest(external_calls=calls), external_egress=True,\n        production_allowed=False, privilege_expansion=False,\n        procedure='agent-reach-external-research',\n    )\n\ndef lock(tmp_path: Path):\n    p=tmp_path/'versions.lock'; p.write_text('IMAGE_REPOSITORY=edarsa/agent-reach-runtime\\nIMAGE_TAG=test-tag\\n',encoding='utf-8'); return p\n\ndef test_service_chains_authorization_policy_activation_and_executor(monkeypatch,tmp_path):\n    fake=SimpleNamespace(success=True,returncode=0,stdout='public result',stderr='',argv=('docker',),query='public trends')\n    monkeypatch.setattr('core.agent_external_research_service.execute_agent_reach_public_search',lambda *a,**k: fake)\n    out=execute_external_research({'allowed':True},'public trends',request(),context(),lock(tmp_path),docker_binary='/usr/bin/docker')\n    assert out.result is fake\n    assert out.evidence.allowed is True and out.evidence.success is True\n    assert out.evidence.procedure=='agent-reach-external-research'\n    assert out.evidence.effective_scopes==('external:web',)\n    assert out.evidence.external_calls_budget==1\n    assert out.evidence.production_touched is False\n    assert 'public trends' not in repr(out.evidence)\n    assert len(out.evidence.query_sha256)==64\n\ndef test_requester_denial_never_calls_executor(monkeypatch,tmp_path):\n    called={'v':False}\n    monkeypatch.setattr('core.agent_external_research_service.execute_agent_reach_public_search',lambda *a,**k: called.__setitem__('v',True))\n    out=execute_external_research({'allowed':False,'reason':'RBAC_DENIED'},'public trends',request(),context(),lock(tmp_path))\n    assert out.result is None and out.evidence.reason=='RBAC_DENIED' and called['v'] is False\n\ndef test_over_budget_denied_policy_first(tmp_path):\n    out=execute_external_research({'allowed':True},'public trends',request(calls=2),context(),lock(tmp_path))\n    assert out.result is None\n    assert out.evidence.allowed is False\n\ndef test_output_limit_is_fail_closed(monkeypatch,tmp_path):\n    fake=SimpleNamespace(success=True,returncode=0,stdout='x'*65537,stderr='',argv=('docker',),query='public trends')\n    monkeypatch.setattr('core.agent_external_research_service.execute_agent_reach_public_search',lambda *a,**k: fake)\n    out=execute_external_research({'allowed':True},'public trends',request(),context(),lock(tmp_path),docker_binary='/usr/bin/docker')\n    assert out.result is None and out.evidence.reason=='AGENT_REACH_OUTPUT_LIMIT_EXCEEDED'\n\ndef test_ai_capability_cannot_enter_external_research_path(tmp_path):\n    out=execute_external_research({'allowed':True},'public trends',request(capabilities=frozenset({'AI_INFERENCE'})),context(),lock(tmp_path))\n    assert out.result is None and out.evidence.allowed is False\n\ndef test_production_remains_fail_closed(monkeypatch,tmp_path):\n    called={'v':False}\n    monkeypatch.setattr('core.agent_external_research_service.execute_agent_reach_public_search',lambda *a,**k: called.__setitem__('v',True))\n    out=execute_external_research({'allowed':True},'public trends',request(),context(),lock(tmp_path),environment=RuntimeEnvironment.PRODUCTION)\n    assert out.result is None and out.evidence.allowed is False and called['v'] is False\n"
    },
    {
      "type": "write_file",
      "path": "backend/tests/test_agent_reach_live_network_canary.py",
      "content": "from pathlib import Path\nimport shutil\n\nfrom core.agent_capability_policy import BudgetRequest, DataClassification, PolicyContext, PolicyRequest, RiskLevel\nfrom core.agent_external_research_service import execute_external_research\n\ndef test_live_public_network_canary():\n    docker=shutil.which('docker')\n    assert docker, 'DOCKER_COMMAND_NOT_AVAILABLE'\n    lock=Path('infra/agent-reach/versions.lock')\n    assert lock.exists()\n    caps=frozenset({'EXTERNAL_RESEARCH'}); scopes=frozenset({'external:web'})\n    ctx=PolicyContext(\n        user_allowed_capabilities=caps,agent_allowed_capabilities=caps,environment_allowed_capabilities=caps,policy_allowed_capabilities=caps,\n        user_allowed_scopes=scopes,agent_allowed_scopes=scopes,environment_allowed_scopes=scopes,policy_allowed_scopes=scopes,\n        budget_limits={'runtime_seconds':60,'files_changed':0,'lines_changed':0,'external_calls':1,'sql_rows':0,'inference_cost_usd':0.0},\n    )\n    req=PolicyRequest(\n        capabilities=caps,scopes=scopes,risk=RiskLevel.R1,data_classification=DataClassification.PUBLIC,\n        budgets=BudgetRequest(external_calls=1),external_egress=True,production_allowed=False,privilege_expansion=False,procedure='agent-reach-external-research',\n    )\n    out=execute_external_research({'allowed':True},'OpenAI official website',req,ctx,lock,timeout_seconds=30,docker_binary=docker)\n    assert out.evidence.allowed is True\n    assert out.evidence.production_touched is False\n    assert out.result is not None\n    assert out.result.success is True\n    assert 0 < out.evidence.stdout_bytes <= 65536\n"
    },
    {
      "type": "write_file",
      "path": "docs/ARCHITECTURE/AGENT_RUNTIME_ACTIVATION_PHASE_CERTIFIED_20260910.md",
      "content": "# EDARSAHUB Agent Runtime Activation — Phase Closure\n\n## Scope\nThis document is the closure artifact for Runtime Activation Gates 5A-5E. It does not authorize Production.\n\n## Certified controls required for closure\n1. Runtime activation fail-closed envelope.\n2. Local isolated Agent Reach runtime executor.\n3. Controlled public egress canary.\n4. Query-driven PUBLIC/R1 external research executor with exactly one external call.\n5. End-to-end service chaining requester authorization, capability/policy, procedure, runtime activation and executor.\n6. Runtime evidence that stores query SHA-256 instead of raw query and records scope, procedure, duration, byte counts, return code and outcome.\n7. Operational limits: external_calls=1, timeout<=30s, output<=65536 bytes.\n8. Explicit AI_INFERENCE isolation from EXTERNAL_RESEARCH path. No OmniRoute runtime installation.\n9. Live network canary executed in Development only.\n10. Production remains forbidden and production_touched must remain false.\n\n## Authority rule\nMore capability never means more privilege. Effective authority remains requested ∩ user_allowed ∩ agent_allowed ∩ environment_allowed ∩ policy_allowed. Procedures and executors cannot grant authority.\n\n## Data and security\nPUBLIC data only for external egress. No raw secrets, cookies, tokens, arbitrary URLs, arbitrary commands, mounts or shell execution are introduced. No SQL write, schema change, MongoDB dependency, Worker/Supervisor/watchdog mutation or RBAC bypass is part of this phase.\n\n## Closure condition\nThe phase is CLOSED only when the Worker terminal for WORKER-AGENT-RUNTIME-ACTIVATION-GATE5E-OPERATIONAL-CLOSURE-20260910 is INTEGRATED + CERTIFIED + 100 + PASS/PASS with no blockers and production_touched=false. Until that terminal exists, this file is a proposed closure artifact, not proof of certification.\n"
    }
  ],
  "checks": [
    {"type": "py_compile", "paths": ["backend/core/agent_external_research_service.py", "backend/tests/test_agent_external_research_service.py", "backend/tests/test_agent_reach_live_network_canary.py"]},
    {"type": "pytest", "paths": ["backend/tests/test_agent_capability_policy.py", "backend/tests/test_agent_procedure_policy.py", "backend/tests/test_agent_reach_policy_adapter.py", "backend/tests/test_agent_execution_gate_e2e.py", "backend/tests/test_agent_runtime_activation.py", "backend/tests/test_agent_reach_runtime_executor.py", "backend/tests/test_agent_reach_egress_canary.py", "backend/tests/test_agent_reach_public_search_executor.py", "backend/tests/test_agent_external_research_service.py", "backend/tests/test_agent_reach_live_network_canary.py"]},
    {"type": "git_diff_check"}
  ]
}